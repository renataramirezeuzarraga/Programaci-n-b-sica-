#3. Calcular el factorial de un número dado.

print ("Programa que calcula el factorial de culquier número")
num = int(input("Introduzca el número: "))

factorial = 1

i = 1
while (i <= num):
    factorial = factorial * i
    i = i + 1

print ("El factorial de " + str(num) + " es " + str(factorial))